spring.application.name=cocktaildb

# PostgreSQL Database Configuration for Local Development
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/cocktaildb}
spring.datasource.username=${DATABASE_USERNAME:cocktaildb}
spring.datasource.password=${DATABASE_PASSWORD:cocktaildb}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable H2 console when using PostgreSQL
spring.h2.console.enabled=false

# Admin Authentication (default password: "admin")
admin.password.hash=${ADMIN_PASSWORD_HASH:$2a$12$kccVkkHnTRyRp9W1oQbWFun6r2DErNX6dy7K0T6d9NHyfqTwLA4VS}

# Barkeeper Authentication (default password: "barkeeper")
barkeeper.password.hash=${BARKEEPER_PASSWORD_HASH:$2a$12$T0sr6.FjEKQv29sedSJKVezIj3/MpIjtvPN3kTt3oVqchyLVAeNca}

# Session Configuration
session.timeout.minutes=${SESSION_TIMEOUT_MINUTES:60}
